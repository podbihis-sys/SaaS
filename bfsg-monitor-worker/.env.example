# Port the worker listens on (Railway injects PORT automatically).
PORT=8080

# Shared secret. Callers must send it as the `x-worker-secret` header.
# Must match SCAN_WORKER_SECRET in the Next.js app.
SCAN_WORKER_SECRET=
